version: "3.9"

services:
  token-ai:

    build: .

    container_name: token_ai_api

    ports:
      - "8000:8000"

    restart: unless-stopped

    env_file:
      - .env

    volumes:
      - ./logs:/app/logs